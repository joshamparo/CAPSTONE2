node_modules/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
dist/
# build/  <-- Commented out to allow root build folder
# frontend/build/ should be included if we want to push it, but usually we don't.
# However, the user explicitly said the build folder is not updated, implying they want it in the repo.
# So I will NOT ignore the root build/ folder.

# But I will ignore local node_modules in subdirectories
**/node_modules/

# Ignore backup and sync folders
backend-github-sync/
backend-push-temp/
backend/
legacy-root-frontend/
scripts/

# Ignore temporary map files and debug files
map_*.js
tmp_*.js
*.txt
