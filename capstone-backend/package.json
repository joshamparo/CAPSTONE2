{
  "name": "backend",
  "version": "1.0.0",
  "description": "Capstone Backend",
  "main": "server.js",
  "scripts": {
    "build": "prisma generate",
    "postinstall": "prisma generate",
    "prestart": "node scripts/bootstrap-schema.js",
    "schema:bootstrap": "node scripts/bootstrap-schema.js",
    "db:push": "prisma db push",
    "start": "node server.js",
    "predev": "node scripts/bootstrap-schema.js",
    "dev": "nodemon --watch server.js --watch routes --watch .env server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@prisma/client": "^6.4.1",
    "@supabase/supabase-js": "^2.103.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "multer": "^2.0.0",
    "pdf-parse": "^1.1.1",
    "pg": "^8.20.0",
    "prisma": "^6.4.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
