{
  "name": "pascualinga-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node capstone-backend/server.js",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd capstone-backend && npm install --no-audit --no-fund && npx prisma generate",
    "build:frontend": "cd frontend && npm install --no-audit --no-fund && npm run build:frontend:only",
    "build:frontend:only": "cd frontend && node ./scripts/run-build.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
