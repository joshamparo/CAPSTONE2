{
  "name": "pascualinga-capstone-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Pascualinga Capstone frontend (dashboards + homepage routes)",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "@supabase/supabase-js": "^2.106.1",
    "lucide-react": "^0.460.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.26.2",
    "recharts": "^2.12.7",
    "web-vitals": "^3.5.2"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.4.8",
    "@testing-library/react": "^16.0.1",
    "@testing-library/user-event": "^14.5.2",
    "cross-env": "^10.1.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider CI=false react-scripts start",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider CI=false GENERATE_SOURCEMAP=false DISABLE_ESLINT_PLUGIN=true react-scripts build",
    "build:frontend:only": "cross-env NODE_OPTIONS=--openssl-legacy-provider CI=false GENERATE_SOURCEMAP=false DISABLE_ESLINT_PLUGIN=true react-scripts build",
    "build:react-scripts": "cross-env NODE_OPTIONS=--openssl-legacy-provider CI=false GENERATE_SOURCEMAP=false DISABLE_ESLINT_PLUGIN=true react-scripts build",
    "test": "cross-env CI=false react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
